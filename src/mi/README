遵循原理：

安装程序尽量简单，整体框架要明了，层次要清晰，层次间分工明确，能简单就不要复杂。

服务端，尽量不要有持久化数据，每一个从client过来的请求无论什么次序都能返回正确结果(安装包这样的有操作流程有
pre install post 的特例)

服务端对于磁盘的挂载，应该是按需挂载，由服务端自己控制，与client 端无关，client端发出向哪个磁盘操作的命令即可。


服务器端，需要有一个 cache 的缓存，以防止多次同样的操作对磁盘的反复挂载。需要在文档中体现和代码中的注释关键字搜索。

